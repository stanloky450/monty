#include "monty.h"
/**
 * f_stack - prints the top level of the stack
 * @head: stack head of stack
 * @counter: line of number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
